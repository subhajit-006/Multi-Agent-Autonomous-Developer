(This is a binary WOFF2 font file. The actual content would be a binary stream of the Inter font in WOFF2 format. Below is a placeholder representation since binary data cannot be directly represented in text format.)

[Binary WOFF2 data for Inter font]